{
  "name": "rmbg-server",
  "version": "1.0.0",
  "description": "A server to process images and remove backgrounds.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "devDependencies": {
    "@gradio/client": "^1.10.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=20.14.0"
  }
}
